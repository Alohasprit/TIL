JOIN :
  - 1개 이상의 테이블에서 데이터를 불러올때 사용하는 명령어
  - 1개 이상의 테이블에서 공통되는 값을 기준으로 데이터를 불러옴
  - ON 명령어와 함께 사용되며 ON 명령어는 불러올 테이블에서 PK와 FK 연결을 설정해 준다
 
  
 ex
 SELECT account.id, account.name, orders.amount
 FROM account
 JOIN orders
 ON account.id = orders.id
 
