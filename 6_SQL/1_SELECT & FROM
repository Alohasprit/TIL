SELECT id, account_id, order_time FROM orders;

1. SELECT 명력어는 컬럼을 선택하는 명력어로서 SELECT 명령어 뒤에 컬럼명을 표기하며, 여러 컬럼을 선택할 때는 ',' 따옴표로 구분한다.
    단, 전체 컬럼을 모두 선택할때는 '*' 표기로 대체 한다.

2. FROM 명령어는 테이블 명을 선택하는 명령어 임

3. 명령어가 종료하는 문장 끝에는 세미 콜론으로 마무리 한다

4. 쿼리는 명령어 및 컬럼명 그리고 테이블 명은 대.소문자 구분을 하지 않는다.
    단, 데이터 값이 문자인 경우는 데이터 값은 대.소문자를 구분한다

5. 컬럼명이나 테이블 명에서 띄워쓰기는 보통 '_'로 대신하면, 굳이 띄워쓰기가 필요할 경우 DBMS에 따라 다를 수 있지만 보통
   "customer name"과 같이 따옴표 또는 [customer name]과 같이 개괄호로 감싸준다 

6. 일반적으로 SQL 쿼리 작성시, 
    명령어(statement)는 대문자로 표기하고, 그 이외는 소문자로 표기 한다.
    ex. SELECT id, account_id, order_time FROM orders;
