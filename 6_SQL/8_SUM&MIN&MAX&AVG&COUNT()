[Aggregator]
  - SELECT statement와 함께 컬럼 값을 aggregation option을 지정해 줌
  - ex: SELECT SUM(*) AS total_sum

1.SUM
  - 컬럼 값의 합을 계산함
  - NULL 값은 제외
  
2. MIN & MAX
  - 컬럼값의 숫자, 날짜, 알파벳 순으로 최소/최대 값을 구함
  - NULL 값은 제외
  
3. AVG
  - 컬럼의 평균 값(mean)을 구함
  - NULL 값은 산정 제외
  
4. COUNT
  - 컬럼 값의 갯수를 산정 함
  - NULL 값은 산정 제외
  
5. 특정 컬럼의 Unique 한 값만 count를 원할 경우
   COUNT(DISTINCT 컬러명)
  
  
  
